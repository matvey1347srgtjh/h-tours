

## Сценарий #1: Уведомление для предстоящего события

**Цель:** Проверить, что система предлагает отправить уведомление для события, которое начнётся в течение 24 часов.

- **Предпосылки:** В базе данных создан тестовый пользователь.
- **Шаги:**
  1. Создать событие с `event_datetime = now + 2 часа`.
  2. Вызвать метод `should_send_notification()`.
- **Ожидаемый результат:** Метод возвращает `True` (уведомление должно быть отправлено).

---

## Сценарий #2: Отсутствие уведомления для отдалённого события

**Цель:** Убедиться, что уведомления не отправляются для событий, которые произойдут не скоро.

- **Предпосылки:** В базе данных создан тестовый пользователь.
- **Шаги:**
  1. Создать событие с `event_datetime = now + 25 часов`.
  2. Вызвать метод `should_send_notification()`.
- **Ожидаемый результат:** Метод возвращает `False` (уведомление не требуется).

---

## Сценарий #3: Отсутствие уведомления для прошедшего события

**Цель:** Проверить, что система не предлагает уведомление для событий, которые уже прошли.

- **Предпосылки:** В базе данных создан тестовый пользователь.
- **Шаги:**
  1. Создать событие с `event_datetime = now - 1 час`.
  2. Вызвать метод `should_send_notification()`.
- **Ожидаемый результат:** Метод возвращает `False`.

---

## Сценарий #4: Граничный случай (ровно 24 часа)

**Цель:** Проверить корректность работы логики на границе диапазона уведомлений.

- **Предпосылки:** В базе данных создан тестовый пользователь.
- **Шаги:**
  1. Создать событие с `event_datetime = now + 24 часа` (точно).
  2. Вызвать метод `should_send_notification()`.
- **Ожидаемый результат:** Метод возвращает `True` (24 часа включительно попадают в диапазон уведомления).

---

## Сценарий #5: Проверка типа возвращаемого значения

**Цель:** Убедиться, что метод `should_send_notification()` возвращает булево значение.

- **Предпосылки:** В базе данных создан тестовый пользователь и событие.
- **Шаги:**
  1. Создать событие с `event_datetime = now + 1 час`.
  2. Вызвать метод `should_send_notification()` и проверить тип результата.
- **Ожидаемый результат:** Возвращаемое значение имеет тип `bool`.

---

## Сценарий #6: Проверка целостности данных в БД

**Цель:** Убедиться, что событие корректно сохраняется в базе данных.

- **Предпосылки:** В базе данных создан тестовый пользователь.
- **Шаги:**
  1. Создать событие с уникальным заголовком "Интеграция".
  2. Выполнить запрос к БД на поиск события с этим заголовком.
- **Ожидаемый результат:** В базе данных находится ровно 1 запись с указанным заголовком.

---


## Фактический результат

```
Ran 6 tests in 0.197s. OK
```



